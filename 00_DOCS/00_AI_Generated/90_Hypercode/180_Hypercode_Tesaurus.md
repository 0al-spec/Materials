# 📚 Hypercode Thesaurus

Полный словарь терминов, технологий, фреймворков и методологий, связанных с Hypercode, HCS и 0AL.

## 1. Ядро проекта

### **Hypercode (.hc)**
Декларативный файл, описывающий логическую структуру приложения с помощью минималистичного, основанного на отступах синтаксиса.

### **Hypercode Cascade Sheets (HCS) (.hcs)**
Конфигурационный слой в YAML-подобном формате, содержащий селекторы и правила для настройки элементов `.hc` с каскадом и контекстом.

### **Resolution Engine**
Механизм, который применяет правила из HCS к структуре Hypercode с учетом специфичности, порядка и контекста.

### **Selector**
Механизм выбора целевых элементов `.hc`:
- **Type Selector** — выбор по имени команды.
- **Class Selector (.class)** — выбор по классу.
- **ID Selector (#id)** — выбор конкретного узла.
- **Child Selector (Parent > Child)** — выбор дочерних элементов.

### **@rules**
Контекстно-зависимые правила (например, `@env[production]`), позволяющие изменять конфигурацию в зависимости от окружения.

### **@import**
Директива для модульного подключения `.hcs` файлов.

### **Mixins/Templates**
Повторно используемые блоки конфигурации для снижения дублирования.

### **Declarative Escape Hatch**
Механизм выхода из декларативной модели к императивной логике (`@factory`, `@expression`).

## 2. Связанные технологии и аналоги

### **Dependency Injection (DI)**
Подход, при котором зависимости передаются объекту извне. Аналоги: **Spring DI**, **Guice**, **Dagger**.

### **Infrastructure as Code (IaC)**
Декларативное описание инфраструктуры:
- **Terraform**
- **Ansible**
- **Pulumi**

### **Policy-as-Code**
Декларативное описание политик безопасности:
- **OPA (Open Policy Agent)** / **Rego**
- **Kyverno**

### **Templating Engines**
Инструменты генерации файлов:
- **Jinja2**
- **Handlebars**
- **Mustache**

### **Feature Flag Systems**
Системы управления функциональностью:
- **LaunchDarkly**
- **Unleash**

### **Wasm Component Model (WIT)**
Стандарт взаимодействия модулей в WebAssembly, полезный для ABI в Hypercode.

### **Model Context Protocol (MCP)**
Протокол взаимодействия LLM с локальными инструментами и данными (Anthropic).

### **A2A (Agent-to-Agent Protocol)**
Протокол взаимодействия агентов из разных сред (Google, Microsoft).

## 3. Инструменты разработки и DX

### **ANTLR**
Генератор парсеров для формальных языков, используемый для синтаксического анализа `.hc` и `.hcs`.

### **VSCode / JetBrains IDEs**
IDE с поддержкой плагинов для подсветки синтаксиса, автодополнения и линтинга.

### **CLI Tools**
Утилиты командной строки для запуска, валидации и инспекции конфигураций.

### **Configuration Inspector**
Инструмент для визуализации каскада правил и их влияния на итоговую конфигурацию.

### **Application Graph Visualizer**
Визуализатор архитектуры приложения, построенной по `.hc`.

### **Linter**
Автоматический анализатор `.hcs` для выявления сложных или некорректных правил.

## 4. Безопасность и доверие

### **Agent Passport**
Подписанный документ, описывающий возможности и ограничения агента (часть 0AL).

### **0AL (Zero-trust Agents Layer)**
Модель выполнения агентов в условиях нулевого доверия:
- **zeroald** — рантайм-демон.
- **agentifyd** — демонизатор устаревших бинарников.

### **Digital Signatures**
Цифровые подписи для проверки целостности `.hcs`.

### **Secret Management**
Подходы к безопасному хранению секретов:
- **HashiCorp Vault**
- **AWS Secrets Manager**
- **Kubernetes Secrets**

## 5. Методологии и концепции

### **Separation of Concerns (SoC)**
Принцип разделения логики и конфигурации.

### **Declarative Programming**
Подход, где описывается *что* должно быть сделано, а не *как*.

### **Zero Trust Security**
Модель, в которой ни один процесс или агент не доверяется по умолчанию.

### **Configuration as Data**
Трактовка конфигураций как данных с формальной схемой и правилами валидации.

### **Explainability**
Возможность объяснить выбор конфигурационного пути (`hcs explain`).

### **Golden Fixtures**
Набор эталонных тестов для проверки совместимости между версиями.

## 6. Языки программирования (для AOT/Interop)

- **Python** — удобен для прототипирования рантайма.
- **Rust** — подходит для безопасного и производительного CLI/рантайма.
- **Java/Kotlin** — интеграция с DI (Spring/Guice).
- **TypeScript/Node.js** — быстрая разработка tooling.
- **Go** — популярный выбор для CLI и облачной интеграции.

## 7. Потенциальные интеграции

- **Kubernetes** (генерация манифестов из `.hcs`).
- **Terraform** (генерация `tfvars`).
- **GitOps** (ArgoCD, FluxCD).
- **Serverless Framework**.