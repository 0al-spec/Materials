# Отличия HCS от DI, YAML, DSL

## 🔍 Зачем это нужно в RFC?

В разделе Core Concepts полезно объяснить, почему .hcs — не просто очередной YAML/DSL/DI-инструмент, а нечто новое. Это помогает:
	•	показать инновацию,
	•	предупредить неверные аналогии у читателя (например, «ну это как YAML для DI»),
	•	и обосновать, зачем нужна новая система (vs использовать существующую).

## 📌 В чём отличия?

| Технология         | Что делает                                                                 | Как .hcs отличается                                                                                 |
|--------------------|------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------|
| DI (Dependency Injection) | Автоматически внедряет зависимости в компоненты, часто на этапе компиляции или старта | `.hcs` не внедряет зависимости напрямую, а настраивает поведение по селекторам и контексту — декларативно и каскадно |
| YAML config        | Просто хранит параметры                                                      | `.hcs` использует селекторы, контекстные правила, переопределения и наследование, как CSS — это не просто ключ-значение |
| DSL (Domain-Specific Language) | Язык для описания поведения в определённой предметной области                    | `.hcs` ближе к стилевому DSL: не кодирует поведение, а стилизует его по условиям и контекстам — подобно CSS, но к логике, а не к виду |