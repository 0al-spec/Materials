```mermaid
mindmap
  root((Hypercode Roadmap))

    Core Language
      (@import, modular .hcs)
      (Advanced selectors)
      (Mixins / templates)
      (Declarative escape hatches: @factory, @expression)
      (Formal grammar & semantics)
      (BNF/EBNF)

    Tooling & Developer Experience
      (Configuration Inspector)
      (IDE plugins: VS Code, JetBrains)
      (Graph visualizer)
      (Linter for .hcs)
      (Debugging tools / Cascade trace)

    Performance & Execution Model
      (AOT compilation)
      (Runner/interpreter)
      (Performance benchmarking)

    Security
      (Secret management: secret://, Vault, AWS Secrets)
      (Signing & verification of configs)

    Documentation & Community
      (Comprehensive guides & tutorials)
      (Best practices)
      (Naming conventions, file structure)
      (Community Q&A, forum)

    Integration & Interoperability
      (Adapters for legacy code)
      (Export to Terraform, K8s, etc.)
      (Pattern catalog for integration)